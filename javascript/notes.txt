javascript:
-----------
               ->it is a scripting and programming language
               ->javascript is the scripting language as well as programming language

why we need js:
---------------
            ->to build dynamic web applications
            ->to add functionality to the web applications
            ->to manipulate DOM(Document object mode) funtionalities
            ->to fetch data from server (API) and also to send data to server Js is required
            

 difference between Java and javascript:
 ---------------------------------------
        java                                                             javascript
   ---------------------------                                    ----------------------------
   ->java is a object oriented programming language            ->js is the scripting language as well as object based programming language
   ->java supports multhithreading                             ->js will support single call stack
   ->java is a imperative in nature                            ->js is a dymanically typed(in js no need to define datatype to the variable)
   ->java is a strictly typed                                  ->js is a weakly typed


   Tokens:
   -----------
        ->Tokens are the smallest unites/elements in the js program

        classifications:
           1.Identifiers
               ->Identifiers are the names given to the code
               ->in the place of identifiers we cant use keywords
               ex:variables names , class names , function names

           2.literals
                 ->literals are the values given to the js code
                 types:
                     1.string literals:
                     2.numeric literals
                     
                  
           3..keywords
                 ->keywords are the predefined words which having its meaning
                 ->in js total 66 keywords present
                 ex:int , char , function , float , boolean , class , const , var , let , if , else , for ,while ,do, continue , switch , break , default , double , return , export , exports , finally , try , catch , super , this , throw , package ,  public , void , null , imnport , get , set , abstract , assert , byte , contant , enum , extends , delete , implements ...etc

           4.separators
                ->separators afre used to separate the given code
                ex: [] , {} , () , ; , : , '' , " " , `` , . , , , <>
           5.operators
                 ->operators are used to perfeorm operations on operands
                 ex:10+20

                 types:
                    1.Assignment operators (=,+=,-=,*=,/=,%=)
                    2.Relational operators  (>,>=,<,<=)
                    3.arthimetic operators  (+,-,*,/,%,**)
                    4.bitwise operators     (&,|,^)
                    5.logic operators       (&&,||,!)
                    6.unary operators       (++,--)
                    7.ternary operators     (condition ? expr1 :expr2)
                    8.shift operators       (>> , <<)
                    9.ccomparision operators (== , === , !=)
           6.comments
                 ->comments are used to comment the given code

                 types:
                       1.single line //
                       2.multi line comment /*

DataTypes in JS:
---------------
      ->DataTypes represents the what type of data we storing inn variables           
                  1.Primitive DataTypes  (immutable)
                       1.number
                       2.string
                       3.boolean
                       4.null
                       5.underline
                       6.symbol
                       7.bigInt
                  2.Non-Primitive DataTypes (mutable, object de-referencing)        
                       1.Arrays
                       2.Class type
                       3.objects
                       4.function              

variables
---------
     ->variables are the named memory location used to store data or values and it can change N number of times during execution

       str="javascript"
       str=10

1.variable declaration
2.variable initialization
3.variable utilization
4.variable re-initialization
5.variable re-declaration

in js we can define variable in 3 ways:
--------------------------------------
        1.let
           ->let variables acts like a local scop/block scop
           ->variable declaration and initialization in  same/different line is possible
           ->re-initialization is possible but re-declaration is not possible
           ->we cant hoist the let variables

       2.var
           ->let variables acts like a global scop/block scop (because var present in windows object)
           ->variable declaration and initialization in  same/different line is possible
           ->re-initialization and re-declaration is possible
           ->we can hoist the var variables
       3.const
           ->const variables acts like like a final (because once initialization values we cant modify data/values)
           ->variable declaration and initialization in  same is possible but different lines is not possible
           ->re-initialization and re-declaration is not possible
           ->we cant hoist the const variable
                

hoisting:
---------
         -> utilizing the variables before declaration
         ->hoisting is possible only through 'var' variable

Strings:
--------
->string are group of characters
->we can declare string 3 ways
 1.""
 2.''
 3.`` ->templates string & string interpolation
        
 string inbuilt functions:
 ------------------------
          
     Arrays:
     --------
          -> arrays are the linear data structures used to homogenous or hetrogenous types of data
          ->Array size is not fixed

 ways to declare the array:
 ------------------------
 1.literal ways
 2.using new keyword(array object)

 function/Methods:
 ----------------
 ->functions is the block of statements which will get execute when ever we called function or invoke
 ->using functions we can increse code readability and code reuseability

 basicall we have to types of functions:
 1.user defined functions:
     ->Any functions that is declared by user and programmer as per there requirements

 2.predefined function:
    -> it means inbuilt functions   


    JS functiontypes:
    ----------------
    1.anonyms function
    2.normal function/named function
    3.functional expression
    4. immediate invoke function/self calling function
    5.arrow function(ES 6)
    6.Nested function
    7.Higher order function
    8.callback function
    9,generator function
