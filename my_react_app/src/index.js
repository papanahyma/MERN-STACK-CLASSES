import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import Nav from "./Nav";

ReactDOM.createRoot(document.querySelector("div")).render(<App/>)
ReactDOM.createRoot(document.querySelector("div")).render(<Nav/>)

